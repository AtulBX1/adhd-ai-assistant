/**
 * ADHD AI Assistant — Frontend API Configuration
 *
 * This file sets the backend API base URL used by main.js.
 *
 * ── Local development ─────────────────────────────────
 * Leave API_BASE_URL as an empty string — the app will
 * fall back to http://127.0.0.1:5000 automatically.
 *
 * ── Production (Render.com) ───────────────────────────
 * After deploying your backend Web Service on Render,
 * replace the empty string below with your backend URL:
 *
 *   window.API_BASE_URL = 'https://adhd-assistant-backend.onrender.com';
 *
 * Then redeploy this static site.
 * ─────────────────────────────────────────────────────
 */
window.API_BASE_URL = 'https://adhd-ai-assistant.onrender.com';
