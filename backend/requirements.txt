# Flask web framework
Flask==3.1.1
Werkzeug==3.1.3

# CORS support (flask-cors, NOT the unrelated 'cors' package)
flask-cors==5.0.1

# Groq API client (LLM backend)
groq==0.28.0

# Environment variable loading
python-dotenv==1.1.0

# Production WSGI server (used by Render)
gunicorn==23.0.0

# HTTP utilities (used by groq client)
httpx==0.28.1
anyio==4.9.0
sniffio==1.3.1
certifi==2025.4.26
httpcore==1.0.9
h11==0.16.0
idna==3.10
